import React, { PureComponent } from 'react';

export default class HeaderComponent extends PureComponent {
    render() {
        return (
            <div>
                <h2>NBU Rate</h2>
            </div>
        );
    }
}
